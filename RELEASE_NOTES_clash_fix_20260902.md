# LKlight 引擎穿模(clash)修复说明

**日期**：2026-09-02
**引擎源码提交**：`a89ff65`（Fix systematic interpenetration in DNA scoring）
**影响平台**：macOS arm64 / Windows x64 / Linux x86-64（三平台同步重建）

---

## 一、问题现象（用户反馈）

PPI 对接结果"界面接触数太多"，肉眼可见分子互相穿插（穿模），怀疑对接是否有效。

## 二、根因（三层，均已量化）

### 1. 引擎 dna 评分系统性奖励穿模（主因，已修复）

LKlight 的 `dna`/`ddna` 评分复刻自官方 PyDockDNA（AMBER94 参数），其中：

| 能量项 | 官方/LKlight 实现 | 后果 |
|---|---|---|
| VDW 排斥 | `(LJ能).min(1.0)` —— 每对深穿透只罚恒定 1.0 | 原子越嵌越深不再追加惩罚 |
| 静电 | `(qi·qj/d²).clamp(±0.012)`，30Å 内全累积 | 异号电荷(RNA 磷酸⁻ vs Lys/Arg⁺)饱和成恒定吸引 |

净效果：**把核酸"插进"蛋白表面时，长程静电净收益 > 局部 VDW 封顶惩罚 → 引擎认为穿模越深打分越好**。

实测（用户真实体系 AF-M9NF14 蛋白 × CR44522 RNA，6 swarm×20×100 步，120 解）：
- 120/120 解全部深 clash（原子间距 < 0.6×vdW 和）：最少 36、中位 138、最多 354 处
- 最大穿透 2.4 ~ 3.4 Å（≈ 一个原子完全扎进另一个原子）
- 评分与穿模正相关：评分 -61 的解 43 处 → 评分 -450 的解 350 处
- "干净"解（深 clash < 20 处）= **0 个**

### 2. 输入配体自带原子重叠（次因，用户数据问题）

真实 RNA 配体（CR44522）输入文件本身有 28 处**磷酸氧(OP1/OP2)与上游残基 O3' 原子重叠 0.64Å**（O3'-P 键长 1.5Å 正常，但 OP 落到了 O3' 位置）——化学上不可能，属建模/转换残留。这部分"穿模"在对接前就存在。

### 3. 长链配体刚性对接的固有局限（体系问题，需应用层引导）

RNA 链长 1324Å vs 受体直径 ~120Å。任何刚性位姿都无法让整条链无 clash 地贴附在蛋白表面 → 该体系不存在"干净解"。**建议**：裁剪配体到结合域（~20-40 nt），或对已知结合区用 restraints。

## 三、修复方案（引擎，1 处常量级改动）

在 `dna.rs` 能量函数加入**重原子线性深嵌惩罚**：

```
对 受体重原子 i × 配体重原子 j：
  若 距离 d < 0.75 × (rᵢ + rⱼ)：罚分 += 6.0 × (0.75×(rᵢ+rⱼ) − d)
```

设计要点：
- **只罚重-重原子对**：跳过氢，不误伤氢键(H···O ~1.8-2.0Å)与极性接触
- **线性**而非硬悬崖：GSO 可沿梯度逃离初始重叠（长链配体初始放置必然穿蛋白，硬悬崖会把全部解锁死——实测 VDW_CUTOFF=1000 时深 clash 反升 36→103）
- 阈值 0.75×vdW 和 ≈ 2.5Å（重原子真实非键接触下限），界面氢键/盐桥/堆积(2.7-3.6Å)不受影响

## 四、验证结果

| 体系 | 修复前 | 修复后 |
|---|---|---|
| **1AZP 蛋白-DNA native 复合物**（正常体系，120 解） | 深嵌位姿能量 -365（穿模被掩盖） | **120/120 解 0 深 clash**，最大穿透 0 |
| 用户蛋白-RNA 长链体系 | 120/120 解 36~354 处深 clash | 最优解 36~44 处（体系上限：1324Å 链刚性对接无干净解），不再被引擎"奖励" |
| 单元测试 test_1azp | -365（穿模位姿） | -1211（深穿透被正确惩罚，期望值已更新） |

## 五、发布物

- 三平台新引擎二进制（含 clash 修复 + 之前全部修复：--noh/--noxt/--now 过滤、Z 窗口加速 36%）
- `clash_analyze.py`：通用 pose clash 体检工具（用法见下）

## 六、用户机器更新

1. 替换 `engine/LKlight.exe` 为本次发布的新引擎（含 clash 修复）
2. 重新跑 PPI 对接 → 正常体系（蛋白-蛋白/短核酸）pose 不再穿模
3. 若配体是 >500Å 的超长核酸链：建议先裁剪结合域片段再对接，或改用复合物建模（AlphaFold-Multimer / 实验结构）
4. 想量化检查任意 pose 是否有穿模，用：
   ```
   python clash_analyze.py pose.pdb lightdock_receptor.pdb
   # 输出：界面接触对/残基对、轻clash、深clash、最大穿透、最严重8对（含残基定位）
   ```
