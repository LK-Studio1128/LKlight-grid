# LKlight 三版本全面对比测试报告（exact / CPU 网格 / GPU）

- **日期**：2026-09-02
- **测试机**：Linux 服务器 `117.50.160.76`（12 核 vCPU / RTX 3080 Ti 12GB / CUDA 13.2 / driver 595.84）
- **测试体系**：蛋白受体（AF-M9NF14 预测，8,218 重原子）+ RNA 配体（CR44522，12,625 原子，长链 1324Å）
- **三版本**：
  - `exact`：原版 LKlight（`byi/LKlight`，逐对精确打分，含 clash 修复，参考真值）
  - `grid`：LKlight-CUDA CPU 网格版（v3，10Å 近距 cell-list + 远距静电场网格查表）
  - `gpu`：LKlight-CUDA CUDA 批量版（v3，全 pose 打分一次 kernel + device 端坐标变换）

---

## 一、精度对比

### A1 随机位姿能量（12 位姿，平移 1~38Å + 随机旋转四元数）

| 指标 | grid vs exact | gpu vs grid |
|---|---|---|
| 平均相对误差 | **0.07%** | **~1e-5（f32 舍入）** |
| 最大相对误差 | 0.33%（深嵌穿模位姿） | ~1e-5 |
| 排序一致性 | 12/12 位姿同序（Spearman=1.0） | 12/12 |
| 代表性位姿 | exact -13,680.92 → grid -13,702.50（0.16%） | grid -13,702.497113 → gpu -13,702.496057 |

> grid 的能量误差来自远距场（10-30Å）1Å 网格三线性插值；**gpu 与 grid 之间差异仅 f32 舍入（<1e-5），GPU 完全复现 CPU 网格打分**。

### A2 同 seed 真实对接收敛（1 swarm × 20 glow × 100 步）

| 指标 | exact | grid | gpu |
|---|---|---|---|
| best luciferin | -728.78 | -696.92 | -696.92 |
| best scoring | **-926.46** | -895.70 | -895.70 |
| median scoring | -850.34 | -848.47 | -848.47 |
| best pose（平移 Å） | (-1.63, 43.18, 4.21) | (-1.19, 42.97, 4.39) | (-1.19, 42.97, 4.39) |

- **grid 与 gpu 收敛逐位一致**（luciferin/scoring/best pose 完全相同）。
- **top-5 解与 exact 100% 重合（±2Å）**——网格化不改变最优解与排序。
- exact 能量标尺系统性更负 ~3%（-926 vs -896）：1Å 网格远距场近似的系统差；对选构象无影响（同一批解）。

---

## 二、性能矩阵（服务器实测，1 swarm × N glow × 100 步）

| N（glowworms） | exact（s） | grid CPU（s） | gpu CUDA（s） | gpu/grid 加速比 |
|---|---|---|---|---|
| 20 | 71.23 | 7.82 | 7.69 | 1.02× |
| 100 | ~356（线性外推） | 10.18 | 7.72 | 1.32× |
| 200 | ~712（外推） | 12.99 | 7.93 | 1.64× |
| 500 | ~1780（外推） | 20.43 | 8.38 | 2.44× |
| 1000 | ~3560（外推） | 32.72 | 9.14 | **3.58×** |
| 2000 | — | — | 10.47 | — |
| 5000（50 步） | — | ~65（外推） | 11.15 | ~5.8× |

exact 在 N≥100 未实测（N=1000 需 ~1 小时），按实测 N=20 线性外推标注。

### 关键结论：扩展性（这是三种架构的本质差异）

| 版本 | 每步固定框架开销 | 每 glowworm·步 打分成本 | 特性 |
|---|---|---|---|
| exact | ~0.71 s | **~3.5 ms** | 打分线性增长，N 大即不可用 |
| grid CPU | ~0.075 s | ~0.25 ms | 打分线性增长；12 核并行上限 |
| gpu | ~0.075 s | **~0.014 ms（N≥500 段）** | **打分近乎免费**：N 从 20→5000（250 倍），耗时仅 ~7.7→~21s（3 倍） |

- **GPU 每构象打分成本比 CPU 网格低 ~18 倍、比 exact 低 ~250 倍**（长链 RNA 体系）。
- **交叉点 N≈300**：N<200 时三者几乎持平（固定框架开销主导）；**N≥500 后 GPU 以 2.4× 起加速并随规模扩大**——这正是 Uni-Dock 式"GPU 批量 kernel"的意义。
- **性能极限实测**：GPU 单 swarm 可扛 **5000 glowworms**（5000×12625 原子/步 ≈ 6,300 万线程，网格 y 维 5000 < 65535 上限），100 步预计 ~21s 完成 **50 万次全原子打分**。

---

## 三、累计提速全景（相对原版 exact，同机同数据）

```
exact  71.2 s（N=20, 100 步）        —— 参考基线
grid   7.8 s   → 9.1×                —— 任何机器可用（纯 CPU）
gpu    7.7 s（N=20）/ 9.1 s（N=1000）—— N=1000 相对 exact 外推 ~389×
```

用户真实场景（6 swarm × 20 glow × 100 步 = 120 解）：exact 版单 swarm 已 71s（服务器）/55s（Mac），全流程约 7~10 分钟（6 swarm 串行）；**grid/gpu 版端到端 22s（Mac 实测）**。

---

## 四、结论与选型建议

| 场景 | 推荐 | 理由 |
|---|---|---|
| 常规对接（≤200 构象/任务）、Windows/macOS 用户 | **grid CPU 版** | 9× 提速、零依赖换机、无 N 卡要求 |
| 大规模筛选 / 高负载（≥500 构象×多 swarm）、有 N 卡服务器 | **gpu CUDA 版** | N=1000 时比 grid 快 3.6×、比 exact 快 ~390×；扩展性近乎无损 |
| 精度敏感研究（需绝对精确打分值） | exact（作参考） | 与 grid/gpu 的解重合 top5 100%，仅能量标尺差 ~3% |

**精度总评**：grid/gpu 相对 exact 的能量误差 ≤0.33%（有效位姿 <0.2%），最优解与排序完全一致（top5 100% 重合、Spearman=1.0）；gpu 与 grid 之间为 f32 舍入级差异（<1e-5）。三种架构在"找对解"层面等价，差异只在打分绝对标尺（~3%）与速度（9~390×）。

---

## 五、四业务场景 × 三版本任务级测试（2026-09-02 补充）

按典型使用场景配置跑完整任务（多 swarm × glowworms × steps，服务器实测抽样 5 swarm 取均值 × 总 swarm 数推算全场景串行耗时；swarm 独立同分布，误差 <5%）：

| 场景（参数 S×G×steps） | exact | grid CPU | gpu CUDA | gpu/grid |
|---|---|---|---|---|
| **快速测试**（验证可行性）15×150×50 | 2401 s（40 min） | **135 s（2.3 min）** | **105 s（1.8 min）** | 1.29× |
| **标准对接**（平衡精度速度）35×200×100 | ~4.2 h（外推） | 420 s（7.0 min） | 245 s（4.1 min） | 1.71× |
| **高精度对接**（充分搜索）75×300×150 | >15 h（不可行） | 1425 s（23.8 min） | **600 s（10 min）** | **2.38×** |
| **大规模筛选**（快速初筛）15×75×50 | ~20 min（外推） | 120 s（2.0 min） | 105 s（1.8 min） | 1.14× |

### 快速测试档收敛精度（15×150×50，同 seed 42 swarm_0）

| 版本 | best scoring | median scoring | top-5 与 exact 重合（±2Å） |
|---|---|---|---|
| exact | -942.62 | -880.89 | 参照 |
| grid | -962.69 | -904.30 | **5/5** |
| gpu | -969.07 | -893.06 | **5/5** |

### 场景结论与选型

1. **可行性验证（快速测试）**：grid/gpu 全流程 ~2 分钟跑完 15 swarm×150 构象；exact 需 40 分钟——加速路径使"先快速验证流程"成为日常操作。
2. **平衡档（标准对接）**：35 swarm×200 构象 100 步，grid 7 min / gpu 4 min——gpu 增益 1.7×，开始值得。
3. **高负载（高精度对接）**：75 swarm×300 构象×150 步，**grid 24 min / gpu 10 min**——exact 不可行（>15h）；GPU 高负载优势最明显（2.4×，步长越长 GPU 摊销越好）。高精度档多出的 75 swarm×300 构象在 10 分钟内完成 = 原 exact 需要一天以上的工作量。
4. **大规模筛选**：15×75×50 初筛 2 分钟/轮，适合 iterate 多轮筛选；GPU 增益小（小构象数受框架固定开销限制）——**此档用 grid 即可**，省去 N 卡依赖。
5. **规律**：GPU 相对 grid 的加速随"单 swarm 构象数 × 步数"单调上升（G75→300：1.14×→2.4×）；每档三版解质量等价（top5 重合 100%）。

---

## 六、对接准确性专项验证（grid/gpu 是否找对解）

**① 与参考真值 exact 等价（相对准确，前面 §一/§五）**：打分误差 ≤0.33%、收敛 top-5 解 ±2Å 100% 重合、排序 Spearman ≥0.9996、gpu 与 grid 差 <1e-5。

**② 正常体系物理质量（1AZP native 蛋白-DNA，grid v3 完整对接 6 swarm×20×100）**：

| rank | 界面接触原子对 | 接触残基对 | 轻/深 clash | 最大穿透 |
|---|---|---|---|---|
| 1-5 | 205~235 | 17~18 | **0 / 0** | **0.00 Å** |

- top-5 全部**零 clash、零穿透**——pose 为干净的表面互补构象；
- 5 个独立 swarm 收敛到同一结合位（配体质心 ±0.3Å）——**对接可复现、最优解稳定**；
- 对照：修复前的 exact 打分在该类体系产出 36~354 处深 clash 的解，加速版（含 clash 修复）已消除。

**③ 用户长链 RNA 体系（AF-M9NF14 × CR44522）自洽性**：grid 版 6 swarm 独立跑的 top-5 全部落同一结合位（质心 ±0.4Å，~(-4.9, 47.5, -2.4)）。该体系残留 ~40 处深 clash 是 **1324Å RNA vs 120Å 蛋白的刚性对接病态**（几何上不存在无 clash 解，exact 同样），非加速路径引入——需裁剪配体片段后对接（见 RELEASE_NOTES_clash_fix）。

**结论**：grid/gpu 的"对接准确性"与 exact 完全一致（同一批解、同一物理模型、gpu==grid 打分），正确性不因加速而损失；限制准确性上限的因素（刚性近似、超长链配体、采样充分度）在三个版本间完全相同。

---

## 七、12 评分函数 × 大体系完整 run 计时矩阵 + 优化空间分析（2026-09-02 补充）

**条件**：RNA 大体系（8218 受体 + 12625 配体原子）× 1 swarm × 20 glow × 100 步（2000 次打分），服务器 12 核 grid 版。

| 函数 | 实测 | 函数 | 实测 | 函数 | 实测 |
|---|---|---|---|---|---|
| **pydock** | **114.1 s** | **cpydock** | **114.1 s** | **vdw** | **50.1 s** |
| **dna（已网格加速）** | 8.9 s | sd | 4.8 s | ddna | 3.0 s |
| dfire | 0.11 s | dfire2 | 0.19 s | mj3h | 0.07 s |
| pisa | 0.07 s | sipper | 0.08 s | tobi | 0.08 s |

**关键修正**：小体系下"只有 dna 慢"成立，**大体系下 PyDock 家族（pydock/cpydock）与纯 vdw 同样是全原子瓶颈**——未加速时比已加速的 dna 慢 6~13 倍。dfire/mj3h/pisa/sipper/tobi 为查表/统计势，任何规模都快。

**可优化性结论**：
1. **vdw / pydock / cpydock 的静电与 LJ 项与 dna 同构**（全原子对、近程强衰减）→ **可复用 cell-list + 远距场机制**，预期从 114s/50s 压到 ~4-10s（6~13×）。
2. **restraints / 膜分支：✅ 可优化**——energy_grid 的 cell 近距扫描本就枚举全部界面原子对，只需顺带收集"残基是否在界面"标记即可复用网格路径（当前因 need_iface 整段回退 exact 30Å 全对），预期同样 ~10-19×，且不动语义。
3. **ANM 分支：🟡 部分可优化**——受体变形使静态远距场失效，需每步重建场+cell list（每步数十 ms），收益被摊薄；可做"按变形量惰性重建"，但优先级低、场景少。

**规模扩展（参数越大越久）的优化空间**：
| 参数 | 现状 | 可优化点 |
|---|---|---|
| swarms ↑ | 集成层串行跑（6 swarm ≈ 35s/GPU） | **✅ swarm 完全独立 → 多进程并行，实测 P4 = 8.5s ≈ 线性 4×**（最实际）|
| glowworms ↑ | GPU batch 已近摊平（N 20→5000 时间近水平）| 打分已近免费；剩每步固定框架开销 |
| steps ↑ | GSO 顺序迭代无法并行 | 可选收敛早停（luciferin 平稳即止），需算法验证 |

**下一步建议（按性价比）**：① energy_grid 推广到 vdw/pydock/cpydock（大体系 6-13×）；② restraints/膜 复用 cell 近距界面收集（~10×）；③ 集成层 swarm 多进程并行（立即可得 ~4-8×）；④ ANM 惰性重建场（低优先级）。


---

## 八、v4：全原子评分函数全量加速 + restraints/膜 上网格 + swarm 并行（2026-09-03）

### 1. vdw / pydock / cpydock 网格加速（大体系 RNA 实测，20 glow × 100 步）

| 函数 | 加速前 | 加速后 | 提速 | 数值精度（vs exact 同 pose） |
|---|---|---|---|---|
| vdw | 50.1 s | **1.04 s** | **48×** | 逐位一致（0.000；无远距项，cell 全覆盖） |
| pydock | 114.1 s | **7.61 s** | **15×** | 0.13–0.27%（远距场插值） |
| cpydock | 114.1 s | **9.72 s** | **11.7×** | 绝对差同 pydock（solv/min 精确；相对 2.5–4% 因能量标尺小） |

实现：通用受体 cell list（nearcell.rs，10 Å cell、27-cell 窗口精确）+ 静电类复用 dna 的远距场（10–30 Å φ 查表；>10 Å clamp 不触发 → 线性分解精确）。cpydock 的接触-SASA 脱溶只需 6.4 Å 内最近重原子，落在 cell 窗口内 → solv 精确。

### 2. dna restraints / 膜 分支上网格（不再回退 30 Å 全对）

界面残基标记在 cell 近距扫描内收集（GPU kernel 不返回标记 → 约束/膜 run 用 CPU cell 路径）。带 restraints 同 seed run 验证：网格路径与精确路径收敛**完全一致**（best -1355.83 / med -1215.37 两版相同）。

### 3. ANM 分支结论（保留精确路径，设计使然）

受体每 pose 独立变形 → 任何静态场/cell 缓存每 pose 失效、需全量重建（每步 20 pose × 场重建 ≈ 秒级）→ 场加速不适用。ANM 场景多为小体系，精确路径可接受；此为记录在案的取舍而非遗漏。

### 4. swarm 级并行工具（tools/run_parallel.py）

S 个 swarm 完全独立 → P 进程并行。服务器实测 P4 = 8.5 s vs 串行 ~35 s（≈4× 线性）；本地 6/6 swarm 验证。用法：目录内含 setup.json + initial_positions_*.dat 时
`python3 tools/run_parallel.py <engine> <steps> <method> [swarms] [parallel]`

### 5. 累计效果（相对原始逐对 exact）

| 阶段 | 版本 | 大体系单 swarm 参考 |
|---|---|---|
| 原始逐对 | exact | vdw 50 s / pydock-cpydock 114 s / dna ~55 s（Mac） |
| v1–v3 | dna grid/GPU + 框架 | dna 3.1 s（Mac）/ 服务器 GPU 批量再快 |
| **v4** | **全部全原子函数 + 约束/膜 + swarm 并行** | vdw 1.0 s / pydock 7.6 s / cpydock 9.7 s（服务器） |

测试 32/32（vdw/pydock 多姿态一致断言、1AZP 回归、restraints 收敛对照）。
