# LKlight-CUDA 加速实测报告（RTX 3080 Ti 服务器真机）

**日期**：2026-09-02
**环境**：Ubuntu 22.04 / RTX 3080 Ti 12GB / Driver 595.84 / CUDA 13.2 (nvcc 13.2.86) / rustc 1.98
**测试体系**：蛋白受体（9796 原子，reduce 加氢）× RNA 配体（12625 原子），dna(AMBER94) 评分，单 swarm 20 glowworms × 100 步

---

## 一、架构（独立副本 `byi/LKlight-CUDA`，原版 `byi/LKlight` 未改动）

参考 UniDock-Pro（CUDA 加速 Vina）的**受体亲和力网格**设计，LKlight 打分拆两段：

| 距离段 | 处理方式 | 说明 |
|---|---|---|
| d ≤ 10 Å（近距） | 逐对精确 | 静电 clamp + LJ(cap 1.0) + 重原子 clash 罚 + 界面标记；Z 窗口 ±10Å |
| 10 < d ≤ 30 Å（远距） | **受体静电场网格** φ(x)=Σq_i/d²（1Å 格点）查表 | 10Å 外 \|q_i·q_j\|/d² < 0.012，clamp 永不触发 → 场线性可分解，**查表结果与逐对精确一致**（仅插值误差） |

代码模块：
- `src/grid_dna.rs` — φ 场构建（环壳 10-30Å 累加）+ 三线性采样（CPU）
- `src/cuda/far_field.cu` — GPU kernel：1 线程/配体原子三线性查表 + atomicAdd 归约
- `build.rs` — `--features cuda` 时 nvcc 编译 kernel → `libfarfield.a` + 链 cudart/stdc++
- `src/gpu_field.rs` — GPU 调用桥，失败**透明回退 CPU**（无 CUDA 机器照常编译运行）
- `dna.rs` — `energy_exact`（参考逐对）与 `energy_grid`（默认快速路径）；无 restraints/膜时跳过隐藏的 O(N_rec×N_lig) 界面全对循环
- 开关：默认 `cargo build --release`（CPU grid）；`cargo build --release --features cuda`（GPU 远距）

## 二、实测数据（同一服务器、同一体系）

| 版本 | 单 swarm 20 glow×100 步 | 相对原始(逐对) |
|---|---|---|
| 原始逐对打分（Z 窗口 30Å，Mac 8 核） | 55.3 s | 1× |
| L1 CPU 网格版 v1（受体扫配体 Z 窗口，Mac） | 7.94 s | 7.0× |
| **L1 CPU 网格版 v2（配体扫受体 cell list，Mac）** | **2.85 s** | **19.4×（累计）** |
| CPU 网格 cell 版（服务器 12 核） | 8.06 s | 较 Z 窗口 grid（16.1s）2.0× |
| **CUDA batch 版（近距+远距一次 kernel）** | 8.08 s（服务器） | 与 CPU cell 持平（打分已非瓶颈） |
| **v3：GSO 框架 + GPU 端变换（服务器）** | n=2000×20 步 **7.57 s**（旧 11.95 s） | **1.58×**；GPU 相对 CPU 3.2× |
| **v3 Mac 端到端（6 swarm×20 glow×100 步全流程）** | **22.0 s** | 原逐对版单 swarm 即 55 s |

**数值一致性**（GPU score = CPU grid score）：
```
Score (DNA) GPU/CUDA:  -15179.263779
Score (DNA) CPU grid:  -15179.263684     差 9.5e-5（f32 舍入）
```
单元测试 31/31（含 1AZP 回归、grid-vs-exact 4 姿态 <2%）。

**GPU 激活确认**（run 时首次成功调用打印）：
```
[gpu_field] CUDA far-field ACTIVE (grid 212x186x189, 12625 lig atoms)
```

## 三、结论与下一步

1. **L1 网格化是本次最大提速（7×）**：把打分从"30Å 全对遍历"变为"10Å 近距逐对 + 远距 O(N_lig) 查表"，所有平台直接受益。
2. **CUDA far-field 已真实运行且数值正确**，但当前粒度（每次打分单次 launch）下相对 CPU grid 无额外收益——实测远距场只占 run 总耗时 <2%（网格化后**近距逐对成为新瓶颈**），单 pose launch 开销 ≈ 计算量。
3. **已完成（2026-09-02 晚）**：近距逐对 GPU 化（受体 10Å cell list）+ **批量 pose kernel**（gridDim.y=N，double atomic 归约）——`Score::batch_energy` 集成进 swarm.update_luciferin。同期把 CPU 近距也改成**配体扫受体 cell list**（去排序、候选对数降 ~5.7×）→ CPU 打分累计 **19.4×（Mac 2.85s）**、服务器 2×。
4. **瓶颈转移 → GSO 框架优化（2026-09-02 晚）**：邻居搜索 O(n²) 改空间分箱（≥64 glowworm 用均匀哈希，cell=最大视野半径，27-cell 窗口精确、id 排序保逐位一致）；位姿快照/邻居安装/概率计算并行化；GPU batch 坐标变换下放 device（每步上传 N×7 pose 参数替代 N×nl×3 全坐标，n=2000 时 303MB→112KB）。同 seed 输出 gso_100.out diff=0（逐位一致）。
5. **n=2000 规模（1 swarm，RTX 3080 Ti）**：GPU 20 步 11.95s（旧）→ 7.57s（新）= 1.58×；CPU 20 步 23.95s → GPU 3.2×。n=200×100 步 GPU 9.34→7.85s；CPU/GPU 收敛统计一致（最佳 -656.2/-654.4，中位 -635.1/-635.3）。
6. **真实任务端到端（Mac，6 swarm×20 glow×100 步 setup+run+rank+generate）**：22.0s 完成 120 解（原逐对版单 swarm 即 55 s）。
7. 部署兼容：`--features cuda` 缺 CUDA 时自动 CPU 回退，不影响原有三平台分发。

## 四、复现命令

```bash
# CPU grid 版（任何机器）
cargo build --release
# CUDA 版（需 NVIDIA GPU + CUDA Toolkit ≥ 13）
cargo build --release --features cuda
# 服务器验证 GPU 激活
LKlight run setup.json initial_positions_0.dat 100 dna   # 首行打印 CUDA ACTIVE
```
